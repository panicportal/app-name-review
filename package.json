{
  "name": "panic-name-review-cloud",
  "version": "12.0.0",
  "private": true,
  "description": "Cross-device name curation studio for 3,333 Panic Survivors",
  "scripts": {
    "test": "node --test tests/*.test.js"
  },
  "engines": {
    "node": "24.x"
  }
}
